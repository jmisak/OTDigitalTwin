# Core dependencies
gradio>=4.0.0
pyyaml>=6.0
matplotlib>=3.5.0
numpy>=1.21.0

# Local AI model inference (required)
transformers>=4.41.0
torch>=2.1.0
accelerate>=0.27.0
sentencepiece>=0.2.0

# Optional API support (not needed for local models)
huggingface-hub>=0.20.0
# anthropic>=0.18.0  # Commented out - not needed for local inference

# Audio features (currently not functional)
# SpeechRecognition
# TTS
# pydub